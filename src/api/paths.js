// API endpoint paths
import { BASE_URL } from "./config";

export const API_PATHS = {
  users: `${BASE_URL}/users`, // GET, POST
  login: `${BASE_URL}/users/login`, // POST
  // Add more endpoints as needed
};
